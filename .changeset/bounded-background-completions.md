---
"@inv1x/pi-background-terminals": patch
---

Avoid output materialization in footer updates, queue bounded completion payloads without spill paths, and keep delayed completion delivery safe after terminal retention expires.
